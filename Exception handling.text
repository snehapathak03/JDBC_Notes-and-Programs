Complete Course Structure
Module 1: Exception Handling (100% Complete)
Introduction to Exception Handling
Errors vs Exceptions
Throwable Class Hierarchy
Types of Exceptions
Checked Exceptions
Unchecked Exceptions
Built-in Exceptions
Exception Propagation
try Block
catch Block
finally Block
Multiple catch
Nested try-catch
Multi-catch (Java 7)
throw Keyword
throws Keyword
Custom Exceptions
User-defined Exceptions
Chained Exceptions
try-with-resources
AutoCloseable Interface
Stack Trace
Assertions
Best Practices
Common Mistakes
Real-world Examples
JDBC Exception Handling
Exception Handling in Spring Boot
100+ Interview Questions
Practice Programs

Chapter 1: Introduction to Exception Handling

What is Exception Handling?Imagine you're writing a Java program.Everything is working perfectly until suddenly:User enters 
a letter instead of a number.File you're trying to read doesn't exist.Database connection fails.Internet connection drops.
Memory becomes full.Should the program immediately stop?If yes,the user loses everything.Instead,Java provides Exception 
Handling, which allows the program to detect, report, and (where appropriate) recover from unexpected problems.
Definition:Exception Handling is a mechanism in Java for detecting, handling, and recovering from runtime problems so that 
the normal flow of the program can continue whenever possible.It separates normal business logic from error-handling logic, 
making programs more robust and easier to maintain.

Why Do We Need Exception Handling?Suppose you write this program:
public class Demo {
    public static void main(String[] args) {
        int a = 10;
        int b = 0;
        int c = a / b;
        System.out.println(c);
        System.out.println("Program End");
    }
}
Output:Exception in thread "main" java.lang.ArithmeticException: / by zero
Notice that:System.out.println("Program End"); never executes.The JVM immediately terminates the program because the 
exception was not handled.Without Exception Handling
Program Starts
      │
      ▼
Execute Statement
      │
      ▼
Exception Occurs
      │
      ▼
Program Terminates

BUT With Exception Handling:
Program Starts
      │
      ▼
Execute Statement
      │
      ▼
Exception Occurs
      │
      ▼
Catch Exception
      │
      ▼
Continue Program
This is the fundamental purpose of exception handling.Real-Life Example:Imagine you're driving a car.Without brakes:
Obstacle
↓
Accident
↓
Journey Ends

With brakes:
Obstacle
↓
Brake Applied
↓
Avoid Accident
↓
Journey Continues
Exception Handling acts like the braking system of a program.It doesn't prevent every problem from occurring, 
but it gives your program a controlled way to respond.

Another Real-Life Example is Suppose you're using an ATM.
Scenario:You insert your ATM card.Enter the PIN.Request ₹5,000.But the ATM has no cash.
Without exception handling:ATM Crashes and/or Screen Turns Off.With exception handling:Sorry!Insufficient Cash.
Please try another ATM.The application continues to work gracefully.

What Exactly is an Exception?An exception is an object that represents an abnormal condition occurring during the 
execution of a program.Many beginners think an exception is just an error message.That's incorrect.An exception is an object.
When something goes wrong, Java creates an object containing information about the problem.For example:int a = 10;int b = 0;
System.out.println(a / b);
Java internally creates an object similar to:ArithmeticException exception = new ArithmeticException("/ by zero");
This object stores details such as:Exception type,Error message,Location of the error,Call stack.

Life Cycle of an Exception:Whenever an exception occurs, Java follows a sequence.
Statement Executes
↓
Problem Occurs
↓
JVM Creates Exception Object
↓
JVM Searches for Handler
↓
Found?
├── YES → Handle Exception → Continue
└── NO → Program Terminates
This sequence is one of the most commonly asked interview topics.

Who Creates Exception Objects?There are two possibilities.
1. JVM Creates It
Example:int x = 10 / 0;The JVM automatically creates:ArithmeticException
2. Programmer Creates It:Using the throw keyword:throw new ArithmeticException("Invalid Input");

Where Can Exceptions Occur?Almost anywhere.Examples include:
Arithmetic:int x = 10 / 0;

Array:int[] arr = new int[5];
System.out.println(arr[10]);

String:String s = null;
System.out.println(s.length());

File:FileReader reader =new FileReader("abc.txt");
If abc.txt doesn't exist, an exception occurs.

Database:Connection con =DriverManager.getConnection(...);
If the database server is unavailable or credentials are wrong, an exception is thrown.

What Happens Without Exception Handling?
public class Test {
    public static void main(String[] args) {
        System.out.println("Start");
        int a = 10 / 0;
        System.out.println("End");
    }
}
Output
Start
Exception in thread "main"
ArithmeticException
Notice:End    never executes.

What Happens With Exception Handling?
public class Test {
    public static void main(String[] args) {
        System.out.println("Start");
        try {
            int a = 10 / 0;
        } catch (ArithmeticException e) {
            System.out.println("Cannot divide by zero");
        }
        System.out.println("End");
    }
}
Output:Start
Cannot divide by zero
End
Now the program continues after handling the exception.

Advantages of Exception Handling
1. Prevents Abnormal Termination:Programs don't have to stop immediately when something unexpected happens.
2. Improves User Experience:Instead of:Program Crashed You can display:Please enter a valid number.
3. Improves Maintainability:Error-handling code is separated from business logic, making programs easier to read and update.
4. Makes Debugging Easier:Exceptions include:Exception type,Message,Stack trace
These details help developers locate and fix problems quickly.
5. Improves Reliability:Applications become more stable because they can respond to common failures gracefully.

Where Is Exception Handling Used?It's used in almost every Java application:Banking software,ATM systems,Hospital management 
systems,E-commerce websites,Railway reservation systems,Spring Boot REST APIs,JDBC applications,Android apps,Desktop 
applications and Cloud services.Whenever an operation can fail, exception handling is relevant.
Common Misconceptions:
❌ "Exceptions are compile-time errors."->No. Most exceptions occur during program execution (runtime).
❌ "Exception and Error are the same."->No.Errors and exceptions are different concepts.
❌ "Exception handling prevents exceptions."No.It handles exceptions after they occur; it doesn't prevent them from happening.

Interview Questions
1. What is Exception Handling?A mechanism in Java used to detect, handle, and recover from runtime problems so that the 
application can continue running whenever appropriate.
2. Why do we need Exception Handling?To prevent abrupt program termination, improve reliability, separate error-handling 
logic from business logic, and provide meaningful feedback to users.
3. Is an exception an object?Yes.Every exception in Java is an object derived from the Throwable class.
4. Who creates exception objects?The JVM creates many built-in exception objects automatically.A programmer can create 
and throw exception objects using the throw keyword.
5. Does every exception terminate the program?No.If an exception is caught by a suitable catch block, the program can 
continue executing.

Key Takeaways
Exception Handling manages unexpected runtime problems.
Exceptions are objects, not just messages.
The JVM creates exception objects for many runtime failures.
If an exception is not handled, the JVM terminates the program.
try and catch blocks allow programs to recover from many exceptions.
Exception handling improves robustness, maintainability, and user experience.

Practice Questions
What is an exception in Java?
Why is exception handling important?
Explain the life cycle of an exception.
Who creates exception objects?
Give five real-world examples where exception handling is required.
What happens when an exception is not handled?
Why are exceptions considered objects?
Can a Java program continue after an exception? Under what condition?

Chapter 2: Errors vs Exceptions & the Complete Throwable Hierarchy
What is Throwable?
Difference between Error and Exception
Checked vs Unchecked Exceptions (overview)
JVM internals of exception creation
Memory representation of exception objects
Complete class hierarchy with diagrams
Interview-focused explanations

Learning Objectives
Understand what Throwable is.
Differentiate between Error and Exception.
Explain the complete Java Exception Hierarchy.
Understand how JVM creates exception objects.
Identify Checked and Unchecked Exceptions.
Explain why Errors should generally not be handled.
Answer MNC interview questions on Exception Hierarchy.

1. Introduction:In the previous chapter, we learned that an exception is an object.Now the question is:Where do these 
exception objects come from?The answer is:All exceptions and errors in Java are descendants of a single root class 
called Throwable.Everything related to exception handling revolves around this class.

2. What is Throwable?Throwable is the root class of Java's exception handling mechanism.Every object that can be thrown 
using the throw keyword or by the JVM must inherit from Throwable.
Definition:Throwable is the superclass of all Errors and Exceptions in Java.Package:java.lang.Throwable
Since java.lang is imported automatically, you don't need to import it explicitly.

3. Why Did Java Introduce Throwable?Suppose Java had no common parent class.ArithmeticException,IOException,NullPointerException,
StackOverflowError.Each would be unrelated.It would be impossible to write generic code like:catch(Throwable t)
The Throwable class provides a common type for all problems.

4. Throwable Hierarchy:The complete hierarchy begins with Object.

                         Object
                            │
                            │
                      Throwable
                     /          \
                    /            \
                Error         Exception
                                 │
                                 │
                       RuntimeException
Every exception and every error ultimately derives from Throwable.

5. Expanded Throwable Hierarchy
Object
│
└────────────── Throwable
               │
     ┌─────────┴─────────┐
     │                   │
   Error             Exception
     │                   │
     │          ┌────────┴─────────┐
     │          │                  │
VirtualMachineException       IOException
     │                  FileNotFoundException
OutOfMemoryError
StackOverflowError

RuntimeException
│
├── ArithmeticException
├── NullPointerException
├── ArrayIndexOutOfBoundsException
├── ClassCastException
├── NumberFormatException
├── IllegalArgumentException
└── IllegalStateException

Exception
│
├── SQLException
├── IOException
├── InterruptedException
├── ParseException
├── ClassNotFoundException
└── ReflectiveOperationException

6. What is Error?An Error represents a serious problem that occurs in the JVM.Errors generally indicate that the JVM 
itself or the execution environment has encountered a condition from which recovery is difficult or impossible.Examples:
Out of Memory,Stack Overflow,JVM Internal Failure
Definition:Errors are serious problems that applications normally should not try to handle.

7. Examples of Errors
OutOfMemoryError--->int[] arr = new int[999999999];
Possible Output:Exception in thread "main"
java.lang.OutOfMemoryError
Reason:The JVM cannot allocate enough memory.

StackOverflowError--->
public class Demo {
    static void test() {
        test();
    }
    public static void main(String[] args) {
        test();
    }
}
Output:java.lang.StackOverflowError
Reason:Infinite recursion fills the call stack.

InternalError:Occurs due to internal JVM failures.
Rarely encountered by application developers.

8. Characteristics of Errors
. Generated by JVM
. Serious system-level issues
. Usually cannot be recovered
. Usually not handled
. Mostly related to memory or JVM resources

9. What is Exception?An Exception represents a condition that occurs during program execution and can often be 
handled gracefully by the application.Examples:Invalid input,Missing file,Division by zero,Database unavailable,Invalid URL.
Unlike Errors, Exceptions are expected to be handled.

10. Examples of Exceptions
ArithmeticException
int a = 10;
int b = 0;
System.out.println(a / b);
Output:ArithmeticException: / by zero

NullPointerException:String name = null;
System.out.println(name.length());
Output:NullPointerException

ArrayIndexOutOfBoundsException
int[] arr = {10,20};
System.out.println(arr[5]);
Output:ArrayIndexOutOfBoundsException

FileNotFoundException
FileReader reader =new FileReader("student.txt");
If the file does not exist:FileNotFoundException

11. Error vs Exception
Feature	                Error	                 Exception
Severity	            Serious	                Less severe
Recovery	            Usually impossible	    Often possible
Generated By	        JVM	                    JVM or programmer
Should be Handled?	    Usually No	            Yes
Package	                java.lang	            java.lang and others
Example	                OutOfMemoryError	    IOException

12. Real-Life Example
Imagine driving a car.
Exception
Tyre Puncture
↓
Repair
↓
Continue Journey
Recoverable.

Error
Engine Exploded
↓
Vehicle Destroyed
Cannot continue.

13. Why Errors Are Rarely Caught
Technically, you can write:
try {
    // code
}
catch(Error e) {
    System.out.println(e);
}
But this is almost never recommended.
Why?If memory is exhausted,how will your program safely recover?It usually cannot.

14. RuntimeException
RuntimeException is a subclass of Exception.
Hierarchy:
Throwable
↓
Exception
↓
RuntimeException
These exceptions occur during execution.
Examples:
ArithmeticException,NullPointerException,ClassCastException,NumberFormatException,IllegalArgumentException,
ArrayIndexOutOfBoundsException.These are called Unchecked Exceptions.

15. Checked Exceptions
Checked exceptions are verified by the compiler.
Examples:IOException,SQLException,FileNotFoundException,InterruptedException,ParseException
ClassNotFoundException,If you don't handle them,your code will not compile.
Example:
FileReader reader =new FileReader("abc.txt");
Compiler:Unhandled exception type FileNotFoundException

16. Unchecked Exceptions
Unchecked exceptions occur at runtime.
Compiler does not force handling.
Example:
int a = 10;
int b = 0;
System.out.println(a / b);
Compiles successfully.
Fails only during execution.

17. JVM Exception Creation Process
Suppose this statement executes:
int x = 10 / 0;
Internally:
Execute Statement
↓
Division by Zero
↓
JVM Detects Problem
↓
Create ArithmeticException Object
↓
Search for Matching catch Block
↓
Found?
YES → Execute catch
NO → Terminate Program

18. Exception Object Internals
An exception object stores information such as:
ArithmeticException
↓
Message
↓
Stack Trace
↓
Cause
↓
Suppressed Exceptions
For example:
ArithmeticException e =
new ArithmeticException("/ by zero");
The object contains:
Type
ArithmeticException
Message
"/ by zero"
Cause
null
Stack Trace
Current execution path

19. Important Methods of Throwable
getMessage() : Returns the exception message.
try {
    int x = 10 / 0;
}
catch(Exception e) {
    System.out.println(e.getMessage());
}
Output:/ by zero

toString()
Returns:ExceptionName: Message
Example:System.out.println(e);
Output:java.lang.ArithmeticException: / by zero

printStackTrace():Prints the complete stack trace.
Example:e.printStackTrace();
Output (simplified):java.lang.ArithmeticException: / by zero
                    at Demo.main(Demo.java:5)
Very useful during debugging.
getCause() : Returns the underlying cause of an exception, if one exists.

20. Memory Representation
Suppose:int x = 10 / 0;
Conceptually:Heap Memory
+--------------------------------------+
ArithmeticException Object
Message : "/ by zero"
Cause : null
Stack Trace : main()
+--------------------------------------+
↓
Reference used by JVM
↓
catch(Exception e)

21. Interview Questions
Q1. What is Throwable?
Answer:Throwable is the root class of Java's exception hierarchy. All exceptions and errors inherit from it.

Q2. What is the difference between Error and Exception?
Answer:Errors indicate serious JVM-level problems that are usually unrecoverable, whereas exceptions represent 
application-level problems that can often be handled.

Q3. Can we catch an Error?
Answer:Yes, it is technically possible because Error extends Throwable. However, it is generally discouraged 
because errors usually indicate unrecoverable conditions.

Q4. Why do all exceptions inherit from Throwable?
Answer:This provides a common parent class, enabling generic exception handling and allowing the JVM to throw 
any problem object consistently.

Q5. What information does an exception object contain?
Answer:An exception object typically contains:Exception type,Error message,Stack trace,Cause,Suppressed exceptions (if any).

Common Mistakes
Confusing Errors with Exceptions.
Assuming every exception is checked.
Catching Throwable or Error indiscriminately in application code.
Ignoring stack traces when debugging.

Best Practices
Catch the most specific exception possible.
Do not catch Throwable unless you have a very specialized framework-level reason.
Avoid catching Error in normal application code.
Use printStackTrace() or, preferably in production applications, a logging framework to record exception details.

Summary
Throwable is the root class of Java's exception mechanism.
It has two major subclasses: Error and Exception.
Errors are serious JVM-related issues and are generally unrecoverable.
Exceptions are application-level problems that can often be handled.
RuntimeException represents unchecked exceptions.
Other subclasses of Exception (such as IOException) are typically checked exceptions.
Exception objects carry useful debugging information like messages and stack traces.

Practice Questions
Draw the complete Throwable hierarchy.
Explain the difference between Error and Exception with examples.
Why does Java use the Throwable class as the root of the hierarchy?
Explain RuntimeException with examples.
What is the difference between checked and unchecked exceptions?
Why should Error generally not be caught?
Explain how the JVM creates and processes an exception object.
What are the important methods of the Throwable class, and what do they do?
Next Chapter: Checked Exceptions vs Unchecked Exceptions (Complete Deep Dive)

Chapter 3: Checked vs Unchecked Exceptions — Complete Deep Dive
1. First Understand the Big Picture
We already learned:
Object
   │
   ▼
Throwable
   │
   ├────────────── Error
   │
   └────────────── Exception
                    │
                    ├────────────── RuntimeException
                    │
                    └────────────── Other Exceptions

Now Exception is broadly divided into:

                    Exception
                       │
             ┌─────────┴─────────┐
             │                   │
       Checked Exception    RuntimeException
                                  │
                           Unchecked Exception

So we have:
Checked Exceptions:Compiler checks them.
Unchecked Exceptions:Compiler does not force you to handle them.

2. What is a Checked Exception?
A checked exception is an exception that the Java compiler requires you to either:Handle using try-catch,
 or Declare using throws. They are checked at compile time.
Example:
import java.io.FileReader;
public class Demo {
    public static void main(String[] args) {
        FileReader file =
                new FileReader("student.txt");
    }
}
This gives a compilation error similar to:Unhandled exception type FileNotFoundException.Why?Because FileNotFoundException 
is a checked exception.

3. Why Does the Compiler Check It?
Imagine you're writing:FileReader file = new FileReader("student.txt");
There is a genuine possibility that:student.txt doesn't exist.
Java designers want you to explicitly decide:"What should my program do if the file doesn't exist?"
So the compiler forces you to handle or declare the exception.

4. Two Ways to Handle a Checked Exception
Method 1: try-catch
import java.io.FileReader;
import java.io.FileNotFoundException;
public class Demo {
    public static void main(String[] args) {
        try {
            FileReader file = new FileReader("student.txt");
        } catch (FileNotFoundException e) {
            System.out.println(
                "File not found"
            );
        }
    }
}

5. Method 2: throws
Instead of handling the exception, you can pass responsibility to the caller.
import java.io.FileReader;
import java.io.FileNotFoundException;
public class Demo {
    static void readFile()
            throws FileNotFoundException {
        FileReader file = new FileReader("student.txt");
    }
    public static void main(String[] args)
            throws FileNotFoundException {
        readFile();
    }
}
We'll study throws deeply in a later chapter.
For now remember:
Checked Exception
        │
        ├── try-catch
        │
        └── throws
		
6. Common Checked Exceptions
IOException
java.io.IOException
Used for input/output failures.

FileNotFoundException
java.io.FileNotFoundException
Occurs when a requested file cannot be opened.

SQLException
java.sql.SQLException
Used for database-related problems.
Extremely important for JDBC.

ClassNotFoundException
java.lang.ClassNotFoundException
Occurs when a class cannot be found while loading it dynamically.

InterruptedException
java.lang.InterruptedException
Common in multithreaded programs when a sleeping/waiting thread is interrupted.

ParseException
java.text.ParseException
Can occur when parsing text into certain date/format representations.

7. Hierarchy of Checked Exceptions
Simplified:
Throwable
   │
   └── Exception
         │
         ├── IOException
         │     │
         │     └── FileNotFoundException
         │
         ├── SQLException
         │
         ├── ClassNotFoundException
         │
         └── InterruptedException
These are checked because they are not subclasses of RuntimeException.

8. What is an Unchecked Exception?
An unchecked exception is an exception that the compiler does not require you to catch or declare.
They are primarily represented by subclasses of:RuntimeException

9. Example of Unchecked Exception
public class Demo {
    public static void main(String[] args) {
        int a = 10;
        int b = 0;
        System.out.println(a / b);
    }
}
Does the compiler complain?No.The program compiles.
When you run it:ArithmeticException occurs.

10. Why Isn't the Compiler Checking This?
Because the compiler cannot reasonably force you to handle every possible programming mistake.
For example:
String name = null;
name.length();
This is generally a programming logic problem.
The compiler cannot determine whether name will actually be null at runtime.
Therefore:
Compile Time
      ↓
No Error
Run Time
      ↓
NullPointerException

11. Common Unchecked Exceptions
Memorize these.
ArithmeticException
10 / 0
NullPointerException
String s = null;
s.length();
ArrayIndexOutOfBoundsException
int[] arr = {10,20};

arr[5];
StringIndexOutOfBoundsException
String s = "Java";

s.charAt(10);
NumberFormatException
Integer.parseInt("abc");
ClassCastException
Object obj = "Java";

Integer number = (Integer) obj;
IllegalArgumentException
Thread.sleep(-100);
Can result in an IllegalArgumentException because the argument is invalid.
IllegalStateException
Occurs when a method is invoked when an object is not in an appropriate state.

12. Hierarchy of Unchecked Exceptions
Throwable
   │
   └── Exception
         │
         └── RuntimeException
                │
                ├── ArithmeticException
                ├── NullPointerException
                ├── ClassCastException
                ├── NumberFormatException
                ├── IllegalArgumentException
                ├── IllegalStateException
                ├── IndexOutOfBoundsException
                │      │
                │      ├── ArrayIndexOutOfBoundsException
                │      └── StringIndexOutOfBoundsException
                └── ...

13. The Most Important Rule
Remember this:If an exception is a subclass of RuntimeException, it is unchecked.If it is a subclass of Exception 
but not RuntimeException, it is generally checked.Example:
IOException
    ↓
Exception
but:
RuntimeException
    ↓
Exception
Therefore:IOException → Checked
RuntimeException → Unchecked     

14. Checked vs Unchecked — Core Difference
Feature						Checked								Unchecked
Checked by compiler?		Yes									No
Must catch/declare?			Yes									No
Main 						superclass	Exception				RuntimeException
Usually represents			Recoverable external conditions		Programming/logic errors
Example						IOException							NullPointerException
Compile-time enforcement	Yes									No

15. Easy Memory Trick
Remember:
Checked:"Compiler says: Handle me!"
Unchecked:"Compiler says: Your responsibility!"

16. Real-World Example
Suppose you're building a banking application.
Database unavailable
Application
     ↓
Database
     X
Connection Failed
This is a possible external condition.
You may handle it:
try {
    // database operation
}
catch (SQLException e) {
    // handle database failure
}
Programming mistake
Account account = null;
account.getBalance();
This is a NullPointerException.
The application developer should fix the underlying logic rather than rely on catching the exception everywhere.

17. Checked Exception Example with JDBC
import java.sql.*;
public class Demo {
    public static void main(String[] args) {
        try {
            Connection con =
                DriverManager.getConnection(
                    "jdbc:mysql://localhost:3306/banking_system",
                    "root",
                    "password"
                );
        } catch (SQLException e) {

            System.out.println(
                "Database connection failed"
            );
        }
    }
}
SQLException is checked.
Therefore Java requires handling or declaration.

18. File Handling Example
import java.io.*;
public class Demo {
    public static void main(String[] args) {
        try {
            FileReader reader =
                new FileReader("data.txt");
        } catch (FileNotFoundException e) {
            System.out.println(
                "File does not exist"
            );
        }
    }
}
FileNotFoundException is checked.
This is one reason File Handling and Exception Handling are closely connected.

19. Unchecked Exception Example
public class Demo {
    public static void main(String[] args) {
        String name = null;
        System.out.println(name.length());
    }
}
Compiler:No compilation error
Runtime:NullPointerException

20. Can We Handle Unchecked Exceptions?
Yes.Unchecked does not mean:"Cannot be handled."It means:"Compiler does not force you to handle it."
You can still write:
try {
    String name = null;
    System.out.println(name.length());
} catch (NullPointerException e) {
    System.out.println(
        "Name cannot be null"
    );
}
So:Unchecked Can be handled{YES} ,Must be handled[ NO]

21. Can We Handle Checked Exceptions?
Yes.And normally you must either handle or declare them.
Checked
    ↓
Must catch OR declare
    ├── try-catch
    │
    └── throws

22. Can We Use throws with Unchecked Exceptions?
Yes.For example:
public static void divide()
        throws ArithmeticException {
    int x = 10 / 0;
}
This compiles.But it is generally unnecessary because ArithmeticException is unchecked.

23. Can We Use try-catch for Checked Exceptions?
Yes.
try {
    FileReader reader =
        new FileReader("abc.txt");
}
catch (FileNotFoundException e) {
    System.out.println("File missing");
}

24. Can We Use try-catch for Unchecked Exceptions?
Yes.
try {
    int x = 10 / 0;
}
catch (ArithmeticException e) {
    System.out.println("Cannot divide by zero");
}

25. Why Does Java Have Checked Exceptions?
This is a very important conceptual interview question.
Java's checked exceptions provide compile-time enforcement for failures that a caller may reasonably be expected to anticipate and handle.
For example:new FileReader("data.txt");
A file may legitimately be absent.Therefore Java forces you to address that possibility.

26. Why Are Runtime Exceptions Unchecked?
Runtime exceptions frequently indicate programming defects or invalid assumptions.
Example:
String name = null;
name.length();
The appropriate fix is often:Fix the program logic
rather than:Catch NullPointerException everywhere

27. Important Interview Trap
Question:Is Exception itself checked or unchecked?
Answer:Exception is the superclass of both checked exceptions and RuntimeException.
So saying simply:"Exception is checked"
is incomplete.
Correct explanation:
Exception
   │
   ├── RuntimeException → Unchecked
   │
   └── Other subclasses → Generally Checked
   
28. Another Interview Trap
Is Error a checked exception?
No.Errors are not exceptions at all.
Hierarchy:
Throwable
   │
   ├── Error
   │
   └── Exception
Error is a separate branch.

29. Compile-Time vs Runtime
Let's compare.
Checked Exception
FileReader reader = new FileReader("abc.txt");
Compiler:
Possible checked exception!
Handle or declare it.
Unchecked Exception
int x = 10 / 0;
Compiler:No exception-handling requirement.
Runtime:ArithmeticException

30. Exception Handling Decision Tree
Use this mental model:
Problem occurs
      │
      ▼
Is it an Error?
      │
   YES│
      ▼
Usually don't catch it
      │
      NO
      │
      ▼
Is it RuntimeException?
      │
   YES│
      ▼
Unchecked
      │
      ▼
Compiler doesn't force handling
      │
      NO
      │
      ▼
Checked Exception
      │
      ▼
Must catch OR declare

31. Important Examples Table
Exception					Type			Why
ArithmeticException			Unchecked		RuntimeException subclass
NullPointerException		Unchecked		RuntimeException subclass
NumberFormatException		Unchecked		RuntimeException subclass
ClassCastException			Unchecked		RuntimeException subclass
IllegalArgumentException	Unchecked		RuntimeException subclass
IOException					Checked			Not RuntimeException
FileNotFoundException		Checked			IOException subclass
SQLException				Checked			Not RuntimeException
ClassNotFoundException		Checked			Not RuntimeException
InterruptedException		Checked			Not RuntimeException
OutOfMemoryError			Error			Error subclass
StackOverflowError			Error			Error subclass

32. Why FileNotFoundException is Checked
Imagine:FileReader reader = new FileReader("student.txt");
There are several possible situations:
student.txt exists
       ↓
Read file
student.txt doesn't exist
       ↓
FileNotFoundException
Because this failure is foreseeable and potentially recoverable, Java requires you to address it.

33. Why NullPointerException is Unchecked
Consider:Student s = null;
s.getName();
The compiler cannot know whether s will be null at runtime.
The correct solution is usually to fix the logic:
if (s != null) {
    System.out.println(s.getName());
}
rather than catching NullPointerException.

34. Checked Exceptions and Method Signatures
Suppose:
static void readFile() {
    FileReader reader = new FileReader("data.txt");
}
Compiler complains.
You can write:
static void readFile()
        throws FileNotFoundException {
    FileReader reader = new FileReader("data.txt");
}
Now the method signature communicates:
"This method may throw FileNotFoundException; callers must deal with it."

35. Exception Propagation Preview
Suppose:
main()
  ↓
methodA()
  ↓
methodB()
  ↓
methodC()

If methodC() throws a checked exception:
methodC()
    ↓
methodB()
    ↓
methodA()
    ↓
main()
The exception can propagate upward until it is handled.

36. Important Rule for throws
For checked exceptions:
Either:Handle it OR Declare it
You cannot simply ignore it.

37. What Happens if You Ignore a Checked Exception?
Example:
static void read() {
    FileReader reader = new FileReader("abc.txt");
}
Compilation fails.This is the fundamental difference between checked and unchecked exceptions.

38. Should Every Exception Be Caught?
No.This is a very important professional programming principle.
Don't do this everywhere:
try {
    // entire application
}
catch(Exception e) {
    // ignore
}
This is bad practice.
Why?Because it:Hides bugs,Makes debugging difficult,Loses useful context,Can cause incorrect application behavior.

39. Best Practice
Catch exceptions at a level where you can actually do something meaningful.
Bad:
catch(Exception e) {
}
Better:
catch(FileNotFoundException e) {
    System.out.println(
        "Please verify the file path."
    );
}
Even better in production:
catch(FileNotFoundException e) {
    logger.error(
        "Unable to open student file",
        e
    );
}

40. Should We Catch RuntimeException?
Sometimes.For example, in a REST API you may have a centralized exception handler that converts known 
application exceptions into appropriate HTTP responses.But don't use try-catch everywhere to hide programming bugs.

41. Professional Java Example
Imagine a banking application:
Controller
    ↓
Service
    ↓
Repository
    ↓
Database

Database failure:
SQLException
     ↓
Repository
     ↓
Service
     ↓
Controller / Global Handler
     ↓
HTTP 500 / appropriate response
This is a realistic enterprise pattern.

42. Checked vs Unchecked: Interview Answer
If the interviewer asks:"What is the difference between checked and unchecked exceptions?"
A strong answer is:Checked exceptions are exceptions other than RuntimeException subclasses that the 
compiler requires the programmer to either catch or declare. They generally represent conditions that 
a caller may reasonably be expected to handle, such as IOException or SQLException. Unchecked exceptions 
are RuntimeException subclasses; the compiler doesn't require them to be caught or declared and they commonly 
indicate programming errors or invalid state, such as NullPointerException or IllegalArgumentException.
That's a much better answer than simply saying:"Checked occurs at compile time and unchecked occurs at runtime."
Why?Because both ultimately occur at runtime. The important distinction is compile-time enforcement, 
not when the exception itself happens.

43. VERY IMPORTANT Correction to a Common Myth
You may hear:"Checked exceptions happen at compile time."[Incorrect.]
The exception itself occurs during execution.

What happens at compile time is that the compiler checks whether you have handled or declared the checked exception.
Correct:Checked Exception
Exception occurs → Runtime
Compiler enforcement → Compile time

44. Another Common Myth
"Unchecked exceptions cannot be handled."[Wrong.]They can be handled.The difference is:
Checked:Must handle or declare
Unchecked:May handle, but compiler doesn't require it.

45. Quick Revision Chart
                         Throwable
                            │
             ┌──────────────┴──────────────┐
             │                             │
           Error                       Exception
             │                             │
       Serious JVM                  ┌──────┴───────┐
       problems                     │              │
                                RuntimeException   Other
                                      │           Exceptions
                                      │              │
                                  UNCHECKED       CHECKED

46. One-Minute Revision
Checked Exception
Compiler checks
       ↓
Must catch OR declare
       ↓
IOException
SQLException
FileNotFoundException
Unchecked Exception
Compiler doesn't require handling
       ↓
RuntimeException
       ↓
NullPointerException
ArithmeticException
NumberFormatException
Error
JVM/System-level problem
       ↓
Usually don't handle
       ↓
OutOfMemoryError
StackOverflowError                                  

47. Interview Questions — Chapter 3
Basic
What is a checked exception?
What is an unchecked exception?
Why are checked exceptions checked by the compiler?
Give five examples of checked exceptions.
Give five examples of unchecked exceptions.
What is RuntimeException?
Is RuntimeException checked or unchecked?
Is Error checked or unchecked?
Can an unchecked exception be handled?
Can a checked exception be ignored?
Intermediate:
Why is IOException checked?
Why is NullPointerException unchecked?
Is Exception itself checked?
What's the difference between compile-time checking and runtime occurrence?
Can throws be used with unchecked exceptions?
Can try-catch handle unchecked exceptions?
Why shouldn't we catch Exception everywhere?
Why should we prefer specific catch blocks?